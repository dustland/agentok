services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "5004:5004"
    env_file:
      - ./api/.env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
